/**
 * Public API of the Services feature.
 *
 * Import from `@/features/services` instead of reaching into internal files so
 * the feature's internal structure can evolve without breaking consumers.
 */
export * from "./api/services.api";
