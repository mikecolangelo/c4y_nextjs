# Cargado automáticamente por Next.js al correr `pnpm build` + `pnpm start`
# (NODE_ENV=production), p.ej. en el VPS. Backend: Strapi del SERVIDOR.
STRAPI_BASE_URL="https://api.car4youpanama.com"
STRAPI_BASE_URL_PORT="443"
